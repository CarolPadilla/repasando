import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProductorecomendadoPage } from './productorecomendado.page';

describe('ProductorecomendadoPage', () => {
  let component: ProductorecomendadoPage;
  let fixture: ComponentFixture<ProductorecomendadoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ProductorecomendadoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
